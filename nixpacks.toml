[publish]
directory = "frontend"

[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
commands = ["cd frontend && npm install"]

[phases.build]
commands = ["cd frontend && npm run build"]

[start]
command = "cd frontend && npx next start"
